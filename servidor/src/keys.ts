import { Pool, createPool, Connection } from 'promise-mysql';
export default{
    database: {
        host: 'localhost',
        user: 'root',
        password: '',
        database: 'labor_docente_bd'
    }
}